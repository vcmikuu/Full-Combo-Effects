#pragma once

#include "config-utils/shared/config-utils.hpp"

DECLARE_CONFIG(ModConfig,
    CONFIG_VALUE(FullComboText, std::string, "FULL COMBO!", "Changes the full combo text on the results screen.");
)